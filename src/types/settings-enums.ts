export enum ThemeMode {
  LIGHT = 'light',
  DARK = 'dark',
  SYSTEM = 'system',
}

export enum Language {
  EN = 'en',
  PT = 'pt',
  PT_BR = 'pt-BR',
  ES = 'es',
}

export enum VibrationPattern {
  GENTLE = 'gentle',
  MODERATE = 'moderate',
  INTENSE = 'intense',
  PROGRESSIVE = 'progressive',
}
