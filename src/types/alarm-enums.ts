export enum Period {
  AM = 'AM',
  PM = 'PM',
}

export enum ChallengeType {
  MATH = 'math',
  MEMORY = 'memory',
  LOGIC = 'logic',
}

export enum DifficultyLevel {
  EASY = 'easy',
  MEDIUM = 'medium',
  HARD = 'hard',
  ADAPTIVE = 'adaptive',
}

export enum BackupProtocolId {
  SNOOZE = 'snooze',
  WAKE_CHECK = 'wakeCheck',
  BARCODE_SCAN = 'barcodeScan',
}
